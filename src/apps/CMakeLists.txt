# Each agent experiment lives in its own subdirectory here. Add new
# experiments by creating a folder with its own CMakeLists.txt and adding
# it below.
add_subdirectory(chat_tui)
add_subdirectory(mini_agent)
